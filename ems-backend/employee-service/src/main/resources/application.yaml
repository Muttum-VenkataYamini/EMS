server:
  port: 8081                        # Standard for employee-service

spring:
  application:
    name: EMPLOYEE-SERVICE          # Must match gateway route lb://EMPLOYEE-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/ems?useSSL=false&serverTimezone=UTC
    username: root
    password: Y@minibalaji81189
  jpa:
    hibernate:
      ddl-auto: update              # or validate in prod
    show-sql: true                  # debug only

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
