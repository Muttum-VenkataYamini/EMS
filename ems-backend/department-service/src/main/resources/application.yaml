server:
  port: 8082                      # Different from others to avoid conflict

spring:
  application:
    name: DEPARTMENT-SERVICE      # Must match what you use in gateway routes
  datasource:
    url: jdbc:mysql://localhost:3306/department_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Y@minibalaji81189
  jpa:
    hibernate:
      ddl-auto: update              # or validate in prod
    show-sql: true                  # debug only
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true       # Helps in Docker/container env later